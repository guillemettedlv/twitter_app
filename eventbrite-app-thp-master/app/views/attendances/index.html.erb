<!-- Jumbotron -->
<div class="jumbotron text-center">
	<h1 class="display-5">Liste des participants à <%="#{@event.title}" %></h1>

  <p class="col-12 text-muted"><%="#{@attendances.count} participants" %></p>

  <p class="col-12 mt-2">
    <%= link_to "Modifier l'evenement", edit_event_path(id: @event.id), class: "btn btn-primary" %>  </p>

  <ul class="list-group list-group-flush mt-3 col-4">
    <% @attendances.each do |attendance| %>
    <li class="list-group-item">
      <%= attendance.user.first_name %> <%= attendance.user.last_name %><br/>
      <span class="text-muted"><%= attendance.user.email %></span>
    </li>
    <% end %>
  </ul>

</div>
</div>
